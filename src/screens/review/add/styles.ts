import { StyleSheet } from 'react-native';
import { AddReviewScreen } from '.';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    padding: 20,
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 20,
  },
  placeName: {
    fontSize: 18,
    marginBottom: 20,
  },
});

export default AddReviewScreen;